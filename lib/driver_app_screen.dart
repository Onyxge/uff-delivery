import "package:flutter/material.dart";

class DriverAppScreen extends StatelessWidget {
  const DriverAppScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Driver Application"),
      ),
      body: const Center(
        child: Text("Driver Application Screen - Coming Soon!"),
      ),
    );
  }
}


